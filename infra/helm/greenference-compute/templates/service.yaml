apiVersion: v1
kind: Service
metadata:
  name: {{ include "greenference-compute.fullname" . }}
  labels:
    app: {{ include "greenference-compute.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8006
      protocol: TCP
      name: http
  selector:
    app: {{ include "greenference-compute.fullname" . }}
